*This project utilizes the superstore data 

#This query was ordering by price for each item
SELECT item_name, price 
FROM superstore 
ORDER BY price
; 

#This query was a statistic about the item prices 
SELECT SUM (price)
FROM superstore 
;

#This query was a statistic about the total pricing in the category 'Kitchen Supplies'
SELECT SUM (price), category
FROM superstore 
WHERE category = 'Kitchen Supplies'
;


This query was for this question: What is the average price people purchased in furnishings?

SELECT AVG (price), category
FROM superstore 
WHERE category = 'Furnishings'
; 
